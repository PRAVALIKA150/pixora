# Taste (Continuously Learned by [CommandCode][cmd])

[cmd]: https://commandcode.ai/

# mssql
- Use `sql.connect(config)` instead of `sql.ConnectionPool.connect(config)` to initialize the mssql connection pool. Confidence: 0.65

